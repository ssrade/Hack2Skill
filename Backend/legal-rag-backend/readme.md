## During thr deployed or taking the code in you machine do the peocess once

# 1. pip install -r requirements.txt
# 2. python -m spacy download en_core_web_lg

# we will add a docker also to make the initalising process more simpler